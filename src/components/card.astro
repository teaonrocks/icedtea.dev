---
const {title, description,href} = Astro.props
---

<div class="flex flex-col gap-2 p-3 border border-neutral-500 rounded-lg group">
    <a href=`${href}` class="text-lg font-semibold group-hover:underline underline-offset-4">{title}</a>
    <p class="text-sm text-neutral-300">{description}</p>
</div>